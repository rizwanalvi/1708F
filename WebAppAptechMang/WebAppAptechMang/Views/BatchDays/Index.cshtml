@model IEnumerable<WebAppAptechMang.BATCHDAY>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DAYS_CODE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DAYS_CODE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DAYID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DAYID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DAYID })
        </td>
    </tr>
}

</table>
